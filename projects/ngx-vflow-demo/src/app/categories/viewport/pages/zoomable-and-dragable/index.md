You can control whether your flow supports zooming and dragging through the `[zoomable]` and `[dragable]` properties.

## Disable zooming

{{ NgDocActions.demoPane("ZoomableDemoComponent") }}

## Disable dragging

{{ NgDocActions.demoPane("DragableDemoComponent") }}
