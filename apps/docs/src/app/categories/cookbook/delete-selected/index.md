This workshop uses `removeNodes` and `removeEdges` to update application-owned collections. Node removal also removes descendants and incident edges.

{{ NgDocActions.demoPane("DeleteSelectedDemoComponent") }}
