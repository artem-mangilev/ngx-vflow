Typed, labeled ports are independent of entity field rows. An ordinary image, native range
input and export button demonstrate rich body content without a form or media engine.
The range input is excluded from graph drag/wheel gestures. Stable port IDs and measured DOM
anchors keep paths aligned when body content changes. This fixture does not process images:
export increments a local counter and width changes the displayed preview metadata.

{{ NgDocActions.demo("PipelineDemoComponent", { container: false }) }}

```typescript file="./pipeline-demo.component.ts"

```
