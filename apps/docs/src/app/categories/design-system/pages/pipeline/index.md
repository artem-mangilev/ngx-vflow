Typed, named image ports compose with application-owned previews and a native range control.
The control excludes drag and wheel gestures; it changes local configuration, not graph position.
The preview is illustrative: this recipe does not process images or execute a pipeline.

{{ NgDocActions.demo("PipelineDemoComponent", { container: false }) }}

```typescript file="../../demos/pipeline-demo.component.ts"

```
