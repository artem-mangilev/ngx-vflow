The frame has its own connection and contains two metric nodes. A note has no ports.
Viewing disables graph editing while retaining focus and viewport navigation.
Metric values and their small CSS bars belong to the application; no chart engine is required.

{{ NgDocActions.demo("RelationshipsDemoComponent", { container: false }) }}

```typescript file="../../demos/relationships-demo.component.ts"

```
