Cards, headers, footers, status indicators, edge labels and a node toolbar use consumer DOM.
Approve is a native button; read-only disables editing while preserving navigation.
Application status and diagnostics are independent of selection and action eligibility.
This composition does not execute a workflow.

{{ NgDocActions.demo("WorkflowDemoComponent", { container: false }) }}

```typescript file="./workflow-demo.component.ts"

```
