Task cards, start/end outlines, XOR and parallel gateways, a pool with two lanes and
sequence/message/association link presentations. External labels and anchors remain unrotated.
Symbols and accessible names are application-owned. This is a visual subset, without XML,
execution, model validation or boundary attachment. Import its primitives from `@vflow/ui/bpmn`.

{{ NgDocActions.demo("BpmnDemoComponent", { container: false }) }}

```typescript file="../../demos/bpmn-demo.component.ts"

```
