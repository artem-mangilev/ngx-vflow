Custom components provide the ability to control handles and their positions.

All you need to do is put the `vflowHandle` directive on an element somewhere in your custom node. In the default `auto` layout the directive computes its position from the parent element, so each handle sits next to its row.

{{ NgDocActions.demoPane("MultipleConnectionPointsDemoComponent") }}
