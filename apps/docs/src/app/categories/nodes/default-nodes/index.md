Simple cards now come from `VflowCardNode` in `@vflow/ui`. Supply plain text in `data.text` and an explicit `ariaLabel`; use an HTML template for richer content.

To get started, you just need to provide `nodes` array to `vflow` component.

{{ NgDocActions.demoPane("DefaultNodesDemoComponent") }}
